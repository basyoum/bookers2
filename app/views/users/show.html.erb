<div>
  <h1>User info</h1>
  <%= image_tag @user.get_profile_image(100,100) %>
  <h4><%= @user.name %></h4>
  <h4><%= @user.introduction %></h4>
  <p>ユーザー情報編集画面へのリンク画像のリンクにする</p>
  <% if @user == current_user %>
  <p><%= link_to "プロフィール編集", edit_user_path(@user) %></p>
  <% end %>
</div>

<p>本の感想投稿欄</p>
<h1>Books</h1>
<% @books.each do |book| %>
<div>
  <%= image_tag @user.get_profile_image(100,100) %>
  <p>Title <%= book.title %></p>
  <p>Opinion <%= book.body %></p>
</div>
<% end %>